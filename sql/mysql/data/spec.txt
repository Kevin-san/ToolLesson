C:/Users/xcKev/git/CorePdfPage/sql/mysql/correct_table/

<p><strong></strong>...</p>
<ul><strong>
<pre><strong><em>
<td><ul><li>
<p><strong>第1步：</strong>
p:strong
ul:strong
pre:strong

翻译功能: https://pypi.org/project/PyDictionary/2.0.1/

The dictionary keys are the different types of the word. If a word is both a verb and a noun then there will be 2 keys:'Noun' and 'Verb'. Each key refers to a list containing the meanings

For Synonyms,

print (dictionary.synonym("Life"))
This will return a list containing the Synonyms of the word.

For Antonyms,

print (dictionary.antonym("Life"))
This will return a list containing the Antonyms of the word.

For Translations,
fr
zh
goslate 
goslate.py is also a command line tool

Translate stdin input into Chinese in GBK encoding

$ echo "hello world" | goslate.py -t zh-CN -o gbk
Translate 2 text files into Chinese, output to UTF-8 file

$ goslate.py -t zh-CN -o utf-8 source/1.txt "source 2.txt" > output.txt
print (dictionary.translate("Range",'es'))



单词字典表

create table IF NOT EXISTS `LanguageDictionary`(
	`LangId` INT UNSIGNED AUTO_INCREMENT,
	`LangValue` VARCHAR(50) NOT NULL,
	`LanguageType` VARCHAR(10) NOT NULL, ( en,jp )
	`ItemType` VARCHAR(20) NOT NULL, ( ... )
	`PartSpeech` VARCHAR(50) NOT NULL, ( ..noun,adj,adv,verb, )
	`PhoneticSymbol` VARCHAR(50) NOT NULL,
	`ChineseTranslation` VARCHAR(200), 
	`ExampleSentence` VARCHAR(200),
	`ExampleSentence2` VARCHAR(200),
	`ExampleSentence3` VARCHAR(200),
	`ExampleSentence4` VARCHAR(200),
	`ExampleSentence5` VARCHAR(200),
	`SamplePattern6` VARCHAR(200),
	`SamplePattern7` VARCHAR(200),
	`SamplePattern8` VARCHAR(200),
	`SamplePattern9` VARCHAR(200),
	`SamplePattern10` VARCHAR(200),
	`DeleteFlag` TINYINT NOT NULL DEFAULT 0,
	`submission_user` VARCHAR(30) DEFAULT 'alvin',
	`submission_date` DATE,
	PRIMARY KEY ( `LangId` )
)ENGINE=InnoDB DEFAULT CHARSET=utf8;

ItemType:
数词 (numbers)
时间 (time)
节日
生日
衣服分类
面料质量
颜色
衣服搭配
款式尺码
帽子
上衣
裤子
裙子
鞋袜
包包
时尚配件
食物 (food)
动物 (animal)
植物 (plant)
水果 (fruit)
公共场所：
各种房间：
身体部位：
性格印象：
日常用品：
常见疾病：
形状尺码：
颜色：
月份：
方向：
位置：
天气：
水果：
蔬菜：
食物：
饮料：
动物：
n.名词 v.动词 pron.代词 adj.形容词adv.副词 num.数词 art.冠词prep.介词conj.连词 int.感叹词,除这十大类词之外,英语还另有判断词yes和no.
分类
　　实词：表示实在意义的词,有名词、动词、形容词、数词、量词、代词、副词.
　　虚词：不表示实在意义而表示语法意义的词,有：介词、连词、助词、叹词.

insert into LanguageDictionary(LangValue,LanguageType,ItemType,PartSpeech,ChineseTranslation,ExampleSentence,ExampleSentence2,ExampleSentence3,ExampleSentence4,ExampleSentence5,SamplePattern6,SamplePattern7,SamplePattern8,SamplePattern9,SamplePattern10,submission_date) values


cmd
H:
cd git\CorePdfPage\media
mklink /D img "I:\图片"
mklink /D audio "I:\音频"
rmdir d:\美女

centos 7.9 系统搭建
jenkins
mariadb
python3
django2
xckevin
xc19901109

上传功能 post
下载功能 get
收藏功能
评论功能
大功能差 音乐和视频
核心差 上传，下载，收藏，评论(可以套用博客 界面),编辑(可以套用博客界面), pdf 分析 (写了一些，卡住了)
课程、小说、博客 使用markdown 编辑 ， 支持下载，上传 ， 设定好权限，（课程原有脑洞保留)
音乐、图片、视频、音频 支持下载，上传 权限不同
工具 无改动 。。。可能有追加功能

爬虫 改为 batch 每天定时跑 ， 小说，图片源自爬虫，音频爬虫还未完成，视频爬虫速度太慢，暂时先缓缓
博客，课程自己写，课程支持生成pdf, 上传也使用pdf ,需要写pdf分析功能   博客自己的
现有的表的数量过多，适当删减 合并

安装服务器系统命令



add upd/add/del function to novel/learn done
merge novel/learn into one table. done

will do
merge image/music/video into one table.
add comment for each function , one table.
authority management.
file upload/download
